# LLMを実行時に使わない「AIっぽい診断」の作り方 — 生活習慣から“運命のお笑い”を当てる仕組みを①〜④で解説

## はじめに

「休日の過ごし方」や「待ち合わせに早く着くか」みたいな、**お笑いと全然関係ない生活習慣の質問**に答えるだけで、あなたに合いそうな芸人・落語家・番組・公式動画を提案する Web 診断を作りました。

- 🔗 デモ: https://fonfon-ai.github.io/unmei-geinin-shindan/
- 構成: React 19 + TypeScript + Vite / 完全静的（GitHub Pages）

「性格診断で芸人をレコメンド」というと、いかにも **裏で LLM に投げてそう** に見えます。が、このアプリは**実行時に LLM にも API にも一切つながっていません**。全部ブラウザの中の「決まった計算」だけで動きます。

この記事では、その仕組みを **①回答 → ②性格 → ③タイプ → ④レコメンド** の4段階で、コード付きで説明します。「AIっぽいUX」を LLM なしで実装したい人の参考になれば。

> ⚠️ このアプリはエンタメ目的の推薦であり、心理検査や医学的診断ではありません。この記事も「そういう作り方もあるよ」という技術ネタです。

---

## 「AI診断」じゃないの？ — 実行時にLLMを使わない設計

先に一番のポイントを。**LLM は“開発時”にしか使っていません。**

- 開発時：質問文・重み・タグ・診断タイプの文言などの**“レシピ”を設計する**のに LLM を活用
- 実行時：そのレシピ（＝固定の数値データと数式）どおりに、ブラウザが**手計算**するだけ

これを担保しているのはコードそのものです。

- ソースに `fetch` / `XMLHttpRequest` / WebSocket が**存在しない**
- CSP で `connect-src 'self'` を宣言 → そもそも外部通信できない
- **ネットを切っても診断は動く**（外部リンクを開くときだけ通信）
- 回答は送信も保存（localStorage / Cookie）もしない

つまり「AIが占ってくれる」ように見えて、中身は **重み付き集計 → 行列変換 → ベクトルの近さ比較** という電卓レベルの処理です。おかげで **再現性がある**（同じ回答なら必ず同じ結果）し、プライバシー的にも安全で、サーバ代もゼロ。

---

## 全体像：4段パイプライン

```
回答
 └①→ 潜在特性ベクトル（12軸: 内向/外向, 計画/即興 …）
      └②→ 笑い嗜好ベクトル（9軸: 意外性, 安心感, 毒, テンポ …）
           ├③→ いちばん近い診断タイプ（全12種）
           └④→ 芸人カタログをスコアリング → 多様性を持たせて選抜
```

キモは、**「ユーザーの好み」と「芸人の芸風」を同じ9次元ベクトルで表現して、コサイン類似度で近さを測る**ところです。ここが噛み合えば、あとは足し算・掛け算で全部つながります。

以下、順番に見ていきます（TypeScript）。

---

## ① 回答 → 潜在特性ベクトル（12軸）

まず、お笑いの好みは**直接聞きません**。誰でも答えられる生活習慣・性格の質問を17問（うち5問は任意）用意し、各選択肢に「潜在特性（トレイト）」への**重み**を持たせます。

トレイトは12軸：

| 軸 | −方向 | +方向 |
|---|---|---|
| extroversion | 内向 | 外向 |
| planning | 即興 | 計画 |
| stimulation | 安定志向 | 刺激志向 |
| analytical | 共感 | 分析 |
| novelty | 保守 | 新奇好き |
| tempoPreference | ゆっくり | 速い |
| … | … | … |

質問はこんな形です。**1つの選択肢が複数の軸に分散して効く**ようにしているのがポイント（後述）。

```ts
{
  id: 'punctual',
  title: '待ち合わせには、いつも?',
  options: [
    { id: 'early', label: '余裕をもって早めに着く',
      weights: { planning: 2, tensionTolerance: -0.5 } },
    { id: 'late',  label: 'ギリギリか、少し遅れがち',
      weights: { planning: -1.5, tensionTolerance: 1, tempoPreference: 0.5 } },
    // …
  ],
}
```

回答が集まったら、選んだ選択肢の重みを軸ごとに合計し、**軸ごとの正規化係数**で割って概ね `-1〜+1` に収めます。未回答・中立選択肢は寄与ゼロなので、**全部スキップしても“中立”として成立**します（エラーにならない）。

```ts
function computeTraits(answers: Answers): TraitVector {
  const raw = zeroTraits();
  for (const q of QUESTIONS) {
    const opt = q.options.find(o => o.id === answers[q.id]);
    if (!opt) continue;                 // 未回答はスキップ
    for (const axis of TRAIT_AXES) {
      raw[axis] += opt.weights[axis] ?? 0;
    }
  }
  // AXIS_SCALE[axis] = 全質問で「その軸に最も強く効く選択肢の絶対値」の合計
  const out = zeroTraits();
  for (const axis of TRAIT_AXES) {
    out[axis] = clamp(raw[axis] / AXIS_SCALE[axis], -1, 1);
  }
  return out;
}
```

### 「1つの回答で結果が決まらない」ようにする

素朴に作ると、ある軸が1問にしか影響されず、**その1問の答えで結果が固定**されてしまいます。これは診断としてつまらないし危うい。

対策として、各軸を複数の質問に分散させ、`正規化係数 = その軸の全質問の最大寄与の合計` で割ることで、**単一回答が1軸に与える影響に上限**を設けています。実際に「単一回答の影響 ≤ 0.6」「ベースラインから1問だけ変えてタイプが変わる割合 < 50%」を**テストで機械的に検証**しています（独裁的な質問がない）。

---

## ② 潜在特性 → 笑い嗜好ベクトル（9軸）

ここが「性格 → 笑いの好み」への翻訳。12のトレイトを、**線形写像（ただの重み付き和）**で9つの「笑い特徴軸」に変換します。

| 笑い軸 | 意味 |
|---|---|
| incongruity | 不一致・意外性 |
| safety | 安心感 |
| edge | 毒・いじり |
| tensionRelease | 緊張と緩和 |
| relational | 関係性・共感 |
| physical | 身体性・リアクション |
| verbal | 言葉・構造・知的 |
| tempo | テンポの速さ |
| worldview | 世界観・シュール |

写像は「係数の表」で定義しておき、掛けて足すだけ。出力は `0〜1` に潰します。

```ts
// 笑い軸ごとに「どのトレイトがどれだけ効くか」を定義
const TRAIT_TO_HUMOR = {
  incongruity: { novelty: 0.5, stimulation: 0.3, analytical: 0.2 },
  safety:      { stimulation: -0.4, tensionTolerance: -0.35, novelty: -0.25 },
  edge:        { tensionTolerance: 0.45, stimulation: 0.3, extroversion: 0.15 },
  tempo:       { tempoPreference: 0.5, stimulation: 0.25, attentionSpan: -0.2 },
  // …9軸ぶん
} as const;

function traitToHumorPreference(traits: TraitVector): HumorVector {
  const pref = zeroHumor();
  for (const axis of HUMOR_AXES) {
    let z = 0;
    for (const [t, w] of Object.entries(TRAIT_TO_HUMOR[axis])) {
      z += w * traits[t];
    }
    pref[axis] = clamp01(0.5 + z);   // 0.5 を中心に上下
  }
  return pref;
}
```

直感的にはこういう変換です：

- 「刺激志向 & 新奇好き」が高い → **意外性・毒** が上がる
- 「疲れ気味 & 安定志向」 → **安心感** が上がる、**テンポ** が下がる
- 「言葉・構造への関心」が高い → **verbal（言葉）** が上がる

ここで大事なのは、**「喫煙してるから毒舌」みたいな単純な直結をしていない**こと。複数回答を統合した“潜在傾向”からしか、笑いの好みを推定しません。

---

## ③ 診断タイプの判定（12種）— コサイン類似度

診断タイプ（例：「王道ど真ん中タイプ」「シュール世界の住人タイプ」）は、それぞれ**9軸空間の“中心ベクトル”**を持っています。

```ts
{
  id: 'surreal-dweller',
  name: 'シュール世界の住人タイプ',
  catchphrase: '意味が分からないのに、なぜか笑える。',
  centroid: hv({ worldview: 0.9, incongruity: 0.6, verbal: 0.4 }), // ← 中心ベクトル
}
```

判定は、ユーザーの嗜好ベクトルと各タイプの中心との**コサイン類似度**を測って、一番近いものを選ぶだけ。コサイン類似度は「ベクトルの向き（＝好みのバランス）」の近さを、大きさに関係なく測れるので相性が良いです。

```ts
function cosineSimilarity(a: HumorVector, b: HumorVector): number {
  let dot = 0, na = 0, nb = 0;
  for (const ax of HUMOR_AXES) { dot += a[ax]*b[ax]; na += a[ax]**2; nb += b[ax]**2; }
  return dot / (Math.sqrt(na) * Math.sqrt(nb));
}

function classifyType(pref: HumorVector, types: DiagnosisType[]): DiagnosisType {
  return types
    .map(t => ({ t, sim: cosineSimilarity(emphasize(pref), t.centroid) }))
    .sort((x, y) => y.sim - x.sim)[0].t;   // 一番近いタイプ
}
```

`emphasize()` は、好みの強い軸をべき乗で少し尖らせて識別性を上げるための小細工です。

> 💡 12タイプをただ並べただけだと「一生選ばれないタイプ」が出がち。なので**「ランダムな回答6000通りで全12タイプに到達できるか」をテストで保証**しています。到達不能なタイプが出たら中心ベクトルを調整、というループで詰めました。

---

## ④ 芸人カタログのランキングと多様性制御

最後に、実在する芸人・落語家をレコメンドします。カタログは**芸人100組＋落語家18名**（＋番組34本）。それぞれに「安心感」「毒」「言葉中心」といった**スタイルタグ**が付いています。

ミソは、**この芸人タグから、ユーザーと同じ9軸ベクトルを生成する**こと。これで「ユーザーの好み」と「芸人の芸風」が同じ土俵に乗ります。

```ts
// タグ → 笑い特徴ベクトルへの寄与テーブル
const TAG_HUMOR = {
  安心感: { safety: 0.5 },
  毒:     { edge: 0.55 },
  言葉中心: { verbal: 0.5 },
  ハイテンポ: { tempo: 0.5 },
  世界観:  { worldview: 0.5 },
  漫才:    { verbal: 0.35, tempo: 0.2 },   // フォーマットも寄与
  落語:    { verbal: 0.45, worldview: 0.3 },
  // …
};

function deriveHumor(entity): HumorVector {
  const v = zeroHumor();
  for (const a of HUMOR_AXES) v[a] = 0.12;   // 低いベース
  for (const tag of [...entity.styleTags, ...entity.primaryFormats]) {
    for (const [a, w] of Object.entries(TAG_HUMOR[tag] ?? {})) v[a] += w;
  }
  // intensity(刺激) / pace(テンポ) / beginnerAccessibility(見やすさ) で微調整
  v.edge += (entity.intensity - 3) * 0.06;
  v.tempo += (entity.pace - 3) * 0.08;
  return clamp01each(v);
}
```

あとはユーザー嗜好との**コサイン類似度でランキング**（同点は id で安定ソート＝決定論）。表示する「相性◯%」は、この類似度を 40〜99 にマッピングした値です。

### 単調にならないための「多様性制御」

トップから機械的に並べると、似た芸人ばかりになりがち。そこで**役割ごとに枠を分けて**選抜します。

- **本命**：総合スコア上位（世代が偏らないよう2組目以降は少しずらす）
- **初心者向け**：`beginnerAccessibility`（見やすさ）が高いもの
- **意外な推薦**：中位で、ユーザーの第1嗜好とは“主軸が違う”もの
- **別世代**：本命と違う世代（昭和/平成/令和）の最上位
- **落語**：落語カテゴリの最上位（初心者向けの演目つき）

```ts
// 例：意外枠 = 中位で、ユーザーの一番強い好みとは主軸が異なる候補
const userTopAxis = dominantAxes(pref, 1)[0];
const surprise = ranked.slice(5, 30)
  .find(e => dominantAxes(e.humor, 1)[0] !== userTopAxis);
```

これで「本命はドンピシャ、でも意外な発見もある」レコメンドになります。各枠が重複しないこと・落語枠が本当に落語であること・別世代枠が本当に別世代であることも、もちろんテスト済み。

---

## 具体例：ある回答を最後まで通してみる

「休日は家でのんびり」「最近ちょっと疲れ気味」「動画は30分でもOK」と答えた人の場合：

1. **①トレイト**：`extroversion` ↓、`stimulation` ↓、`attentionSpan` ↑
2. **②笑い嗜好**：`safety`（安心感）↑、`tempo`（テンポ）↓、`verbal`（言葉）やや↑
3. **③タイプ**：安心感・言葉寄りの中心ベクトルに最も近い →「王道ど真ん中タイプ」や「じっくり玄人タイプ」あたり
4. **④レコメンド**：`safety`/`verbal` の高い芸人（王道漫才・じっくり系）が上位に。落語枠も相性よく出る

逆に「新しい店はすぐ入る」「元気、刺激がほしい」だと `stimulation`/`novelty` が上がり、`incongruity`（意外性）・`edge`（毒）寄りのタイプと芸人に振れます。**入力を変えると、ちゃんと連続的に結果が動く**のがこの方式の気持ちいいところ。

---

## 決定論であることを“テストで”守る

ロジックが数式なので、性質を機械的にテストできます（Vitest・全35件）。代表的なもの：

- ✅ 同じ回答は必ず同じ主結果（**決定論**）
- ✅ ランダム回答6000通りで**全12タイプに到達可能**
- ✅ 全項目スキップでも落ちない（中立で成立）
- ✅ **単一回答が結果を支配しない**（影響上限・タイプ変化率 < 50%）
- ✅ 表示URLは https の許可ドメインのみ（安全性）

重みや写像をいじると、これらの性質が崩れることがあります。**「性質をテストで固定して、パラメータは安心して調整する」**——これが数式ベース診断の運用のコツでした。

---

## なぜ実行時に LLM を使わなかったのか

「LLMに丸投げ」でも作れます。あえて使わなかった理由：

| 観点 | 数式ベース | 実行時LLM |
|---|---|---|
| 再現性 | ◎ 同じ入力=同じ出力 | △ ぶれる |
| プライバシー | ◎ 回答を外に出さない | △ 送信が必要 |
| オフライン | ◎ 動く | ✗ |
| コスト/速度 | ◎ ゼロ・即時 | △ 従量・レイテンシ |
| 説明可能性 | ◎ 全部たどれる | △ ブラックボックス |
| 表現の自由度 | △ 設計が要る | ◎ 柔軟 |

診断アプリは「同じ人が何度やっても同じ」「回答を送りたくない」が効いてくる領域。なので **設計に LLM を使い、実行は決定論**、という役割分担にしました。LLMは“無限の表現力”というより“**優秀な設計アシスタント**”として使うと相性がいい、という学びです。

---

## まとめ

- 生活習慣の回答を、**①トレイト → ②笑い嗜好** に変換
- **③診断タイプ** も **④芸人** も、同じ9次元ベクトルにして**コサイン類似度**で近さを測る
- 仕上げに**多様性制御**で単調さを回避
- 全部“決まった計算”なので、実行時にLLM不要・オフライン可・再現性あり・テストで性質を保証

「AIっぽい体験」は、必ずしも実行時のAIを必要としません。**ベクトル化 + 類似度 + ちょっとの多様性制御**で、けっこう楽しい診断が作れます。

コードは全部 OSS で公開しています。よかったら覗いてみてください 👇

- リポジトリ: https://github.com/fonfon-ai/unmei-geinin-shindan
- デモ: https://fonfon-ai.github.io/unmei-geinin-shindan/

読んでいただきありがとうございました！
